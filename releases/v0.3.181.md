Add configurable status polling interval setting

- Add pollingInterval to DEFAULT_SETTINGS (default 100ms)
- Replace hardcoded 100ms in startPolling() with configurable value
- Add updatePollingInterval() method to restart polling on change
- Trigger polling restart from settings PATCH route
- Add Status Polling Interval dropdown in Settings > General (50/100/150ms)
